{
  "name": "Api3ServerV1",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen $SUBGRAPH_FILE",
    "build": "graph build $SUBGRAPH_FILE",
    "deploy": "graph deploy $SUBGRAPH_NAME $SUBGRAPH_FILE --ipfs $IPFS_NODE --node $SUBGRAPH_NODE --version-label $VERSION_LABEL"
  }
}
